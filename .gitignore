# ============================================================
#  공개 저장소 안전장치 — docs/ (암호화된 리포트)만 올린다.
#  나머지는 전부 로컬 전용 (개인정보·스크립트·데이터 보호)
# ============================================================

# 전부 무시하고
/*
# docs 폴더와 필수 파일만 예외로 허용
!/docs/
!/.gitignore
!/README.md

# docs 안에서도 혹시 모를 원본은 차단 (암호화된 월 리포트 + 인덱스만)
docs/dashboard_full.html
docs/report_data.json
